# example_repo/setup.py
from setuptools import setup

setup(
    name='example-repo',
    version='0.1',
    packages=['example_repo'],
    install_requires=[
        # List any dependencies here
    ],
)

